import './Hero.css'

export default function Hero() {
  return (
    <section className="hero">
      <div className="wrap hero__inner">
        <h1 className="hero__title">
          Equipment built to disappear,
          <br />
          so the sound doesn't have to.
        </h1>
        <p className="hero__sub">
          Headphones, speakers, turntables and amplifiers — designed in-house,
          measured obsessively, sold without the markup of a showroom.
        </p>
      </div>
    </section>
  )
}
